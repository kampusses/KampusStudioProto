@page
@model GenerateRecoveryCodesModel
@{
    ViewData["Title"] = "Genera codici di ripristino per l'autenticazione a due fattori (2FA)";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h4>@ViewData["Title"]</h4>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Metti questi codici in un luogo sicuro.</strong>
    </p>
    <p>
        Se perdi il tuo dispositivo e non hai i codici di ripristino perderai l'accesso al tuo account.
    </p>
    <p>
        La generazione di nuovi codici di ripristino non modifica le chiavi utilizzate nelle app di autenticazione. Se desideri modificare la chiave
        utilizzata in un'app di autenticazione, devi <a asp-page="./ResetAuthenticator">reimpostare le chiavi di autenticazione.</a>
    </p>
</div>
<div>
    <form method="post" class="form-group">
        <button class="btn btn-danger" type="submit">Genera codici di ripristino</button>
    </form>
</div>